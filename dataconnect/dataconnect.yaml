specVersion: "v1beta"
serviceId: "masteradmin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "masteradmin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
